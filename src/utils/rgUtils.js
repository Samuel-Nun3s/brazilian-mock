export function generateRG() {
  const rgArray = [1, 2, 3, 4, 5, 6, 7, 8];

  // for (let i = 0; i < 8; i++) {
  //   cpfArray.push(Math.floor(Math.random() * 10));
  // }

  let rg = getCheckDigit(rgArray);
}

function getCheckDigit(rgArray) {
  
}